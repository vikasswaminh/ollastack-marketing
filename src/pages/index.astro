---
const APP = "https://login.ollastack.com";
const SITE = "https://ollastack.com";
const HOME_TITLE = "Ollastack — Forms, mailboxes & email testing, built for your AI agents";
const HOME_DESC =
  "Forms, mailboxes & email testing, built for your AI agents. Three things your agents need, one backend: form endpoints they POST to and sail past the bot defenses, real mailboxes they send, receive, reply, and read OTP codes from, and disposable inboxes for testing email in CI.";

const orgLd = {
  "@context": "https://schema.org",
  "@type": "Organization",
  name: "Ollastack",
  url: `${SITE}/`,
  logo: `${SITE}/og.png`,
  description: HOME_DESC,
};

const siteLd = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  name: "Ollastack",
  url: `${SITE}/`,
  description: HOME_DESC,
};
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{HOME_TITLE}</title>
  <meta name="description" content={HOME_DESC}>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=DM+Mono:wght@400;500;600&display=swap" rel="stylesheet">
  
  <script type="application/ld+json" set:html={JSON.stringify(orgLd)} is:inline />
  <script type="application/ld+json" set:html={JSON.stringify(siteLd)} is:inline />

  <style is:global>
    /* CSS RESET & VARIABLES */
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      /* Brand colors — Bold Red matching Blog */
      --brand-red: #DC2626;
      --brand-red-hover: #B91C1C;
      --brand-red-light: #FEF2F2;
      --brand-red-border: #FCA5A5;
      --brand-red-glow: rgba(220, 38, 38, 0.15);

      /* Primary neutrals */
      --text-main: #09090B;
      --text-muted: #64748B;
      --text-sub: #475569;
      --text-light: #94A3B8;

      /* Surfaces & Borders */
      --bg-page: #FFFFFF;
      --bg-subtle: #FAFAFB;
      --bg-card: #FFFFFF;
      --border-color: #E2E8F0;
      --border-light: #F1F5F9;

      /* Accent highlights */
      --blue-badge-bg: #EFF6FF;
      --blue-badge-text: #2563EB;
      --green-badge-bg: #ECFDF5;
      --green-badge-text: #059669;

      /* Typography */
      --font-main: 'Plus Jakarta Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      --font-mono: 'DM Mono', 'SFMono-Regular', Menlo, Monaco, Consolas, monospace;

      /* Radius */
      --radius-pill: 9999px;
      --radius-card: 16px;
      --radius-inner: 10px;

      /* Transitions & Shadows */
      --shadow-card: 0 4px 20px -2px rgba(0, 0, 0, 0.05), 0 2px 6px -1px rgba(0, 0, 0, 0.03);
      --shadow-hover: 0 12px 32px -4px rgba(0, 0, 0, 0.09), 0 4px 12px -2px rgba(0, 0, 0, 0.04);
      --transition-fast: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
    }

    html {
      scroll-behavior: smooth;
      font-size: 16px;
    }

    body {
      font-family: var(--font-main);
      color: var(--text-main);
      background-color: var(--bg-page);
      background-image: none !important;
      line-height: 1.5;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    ul, ol {
      list-style: none;
    }

    button {
      font-family: inherit;
      border: none;
      background: none;
      cursor: pointer;
    }

    /* CONTAINER */
    .container {
      max-width: 1280px;
      margin: 0 auto;
      padding: 0 24px;
    }

    /* TOP ANNOUNCEMENT BAR */
    .announce-bar {
      background: #FFFFFF;
      border-bottom: 1px solid var(--border-color);
      padding: 10px 0;
      font-size: 0.85rem;
      color: var(--text-sub);
    }

    .announce-inner {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      text-align: center;
    }

    .announce-dot {
      font-size: 0.8rem;
    }

    .announce-text strong {
      color: var(--text-main);
      font-weight: 700;
    }

    /* NAVBAR */
    .navbar {
      position: sticky;
      top: 0;
      z-index: 100;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border-light);
      padding: 16px 0;
      transition: box-shadow 0.2s ease;
    }

    .nav-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .logo-lockup {
      display: flex;
      align-items: center;
      gap: 8px;
      text-decoration: none;
    }

    .logo-box {
      width: 32px;
      height: 32px;
      background: var(--brand-red);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 2px 8px rgba(225, 29, 72, 0.35);
    }

    .wordmark {
      font-size: 1.35rem;
      font-weight: 800;
      letter-spacing: -0.03em;
      color: var(--text-main);
    }

    /* Centered Navigation Links */
    .nav-pill {
      display: flex;
      align-items: center;
      gap: 4px;
      background: rgba(248, 250, 252, 0.95);
      border: 1px solid rgba(226, 232, 240, 0.95);
      padding: 4px 6px;
      border-radius: var(--radius-pill);
      box-shadow: 0 1px 4px rgba(0, 0, 0, 0.03);
      backdrop-filter: blur(8px);
    }

    .nav-link {
      font-size: 0.88rem;
      font-weight: 600;
      color: #475569;
      padding: 7px 15px;
      border-radius: var(--radius-pill);
      transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
      display: inline-flex;
      align-items: center;
      gap: 5px;
      text-decoration: none;
    }

    .nav-link:hover {
      color: #09090B;
      background: #FFFFFF;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
    }

    .chevron {
      font-size: 0.72rem;
      opacity: 0.65;
      transition: transform 0.2s ease, color 0.2s ease;
      display: inline-block;
    }

    .nav-dropdown-wrap {
      position: relative;
    }

    .nav-dropdown-wrap:hover .chevron {
      transform: rotate(180deg);
      color: var(--brand-red);
    }

    /* Right Nav Buttons */
    .nav-right {
      display: flex;
      align-items: center;
      gap: 20px;
    }

    .nav-signin {
      font-size: 0.92rem;
      font-weight: 600;
      color: var(--text-main);
      transition: color 0.15s ease;
    }

    .nav-signin:hover {
      color: var(--brand-red);
    }

    /* Pill Buttons */
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      font-weight: 600;
      border-radius: var(--radius-pill);
      transition: var(--transition-fast);
      cursor: pointer;
      white-space: nowrap;
    }

    .btn-dark-pill,
    .btn-red-pill {
      background: var(--brand-red);
      color: #FFFFFF;
      padding: 9px 20px;
      font-size: 0.9rem;
      font-weight: 700;
      border: 1px solid var(--brand-red);
      box-shadow: 0 2px 8px rgba(220, 38, 38, 0.25);
    }

    .btn-dark-pill:hover,
    .btn-red-pill:hover {
      background: var(--brand-red-hover);
      border-color: var(--brand-red-hover);
      transform: translateY(-1px);
      box-shadow: 0 4px 14px rgba(220, 38, 38, 0.35);
    }

    .btn-white-pill {
      background: #FFFFFF;
      color: var(--text-main);
      padding: 9px 20px;
      font-size: 0.9rem;
      font-weight: 600;
      border: 1px solid var(--border-color);
    }

    .btn-white-pill:hover {
      border-color: #cbd5e1;
      background: var(--bg-subtle);
      transform: translateY(-1px);
    }

    .btn-hero {
      padding: 12px 28px;
      font-size: 0.98rem;
      font-weight: 700;
    }

    .btn-full {
      width: 100%;
    }

    .arrow {
      transition: transform 0.15s ease;
    }

    .btn:hover .arrow {
      transform: translateX(3px);
    }

    .nav-burger {
      display: none;
      background: transparent;
      padding: 4px;
    }

    .nav-mobile {
      display: none;
    }

    /* HERO SECTION (TIGHTENED WHITESPACE) */
    .hero-section {
      padding: 32px 0 24px 0;
      overflow: hidden;
      text-align: center;
    }

    .hero-layout {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    .hero-content {
      max-width: 860px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .hero-tag-badge {
      display: inline-flex;
      align-items: center;
      gap: 16px;
      background: #F1F5F9;
      color: #475569;
      padding: 6px 18px;
      border-radius: var(--radius-pill);
      font-size: 0.76rem;
      font-weight: 700;
      letter-spacing: 0.05em;
      margin-bottom: 20px;
      white-space: nowrap;
    }

    .badge-feature-item {
      display: inline-flex;
      align-items: center;
      gap: 6px;
    }

    .badge-blue-dot {
      width: 6px;
      height: 6px;
      background: #3B82F6;
      border-radius: 50%;
      flex-shrink: 0;
    }

    .hero-main-title {
      font-size: clamp(2.4rem, 4.2vw, 4.1rem);
      font-weight: 800;
      line-height: 1.15;
      letter-spacing: -0.035em;
      color: var(--text-main);
      margin-bottom: 18px;
      text-align: center;
    }

    .nowrap-headline {
      white-space: nowrap;
    }

    .text-brand-red {
      color: var(--brand-red);
      font-weight: 900;
      font-size: 1.16em;
      display: inline-block;
      letter-spacing: -0.04em;
      text-shadow: 0 4px 24px rgba(220, 38, 38, 0.22);
    }

    .hero-description {
      font-size: 1.12rem;
      line-height: 1.65;
      color: var(--text-sub);
      max-width: 720px;
      margin: 0 auto 28px auto;
      text-align: center;
    }

    .hero-cta-buttons {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 14px;
      margin-bottom: 28px;
    }

    .hero-geo-callout {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: #F0F7FF;
      border: 1px solid #BAE6FD;
      border-radius: var(--radius-pill);
      padding: 9px 20px;
      margin: 0 auto;
    }

    .geo-pin {
      font-size: 1.1rem;
      flex-shrink: 0;
    }

    .geo-text {
      font-size: 0.88rem;
      color: #0369A1;
      line-height: 1.45;
    }

    .geo-text strong {
      color: #0284C7;
      font-weight: 700;
    }

    /* HERO RIGHT: ARCHITECTURE DIAGRAM */
    .hero-right {
      position: relative;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .diagram-hub-wrapper {
      position: relative;
      width: 740px;
      height: 560px;
    }

    .hub-connector-svg {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 1;
      pointer-events: none;
    }

    /* Central Hub Card (OLLASTACK) */
    .center-hub-card {
      position: absolute;
      top: 190px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 4;
      width: 190px;
      height: 180px;
      background: #FFFFFF;
      border: 2px solid #FCA5A5;
      border-radius: 18px;
      padding: 20px 14px;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      box-shadow: 0 10px 32px rgba(220, 38, 38, 0.08), 0 2px 6px rgba(0, 0, 0, 0.03);
      transition: var(--transition-fast);
    }

    .center-hub-card:hover {
      transform: translateX(-50%) translateY(-2px);
      border-color: #DC2626;
      box-shadow: 0 14px 40px rgba(220, 38, 38, 0.16);
    }

    .hub-icon-wrap {
      width: 42px;
      height: 42px;
      background: var(--brand-red);
      border-radius: 10px;
      margin: 0 auto 10px auto;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 14px rgba(220, 38, 38, 0.35);
    }

    .hub-brand-name {
      font-size: 1.05rem;
      font-weight: 800;
      letter-spacing: 0.05em;
      color: var(--text-main);
      margin-bottom: 8px;
    }

    .hub-stack-points {
      display: flex;
      flex-direction: column;
      gap: 3px;
      font-size: 0.64rem;
      font-weight: 700;
      letter-spacing: 0.04em;
      color: #64748B;
    }

    /* Satellite Cards */
    .sat-card {
      position: absolute;
      z-index: 3;
      background: #FFFFFF;
      border: 1px solid var(--border-color);
      border-radius: 14px;
      padding: 12px 14px;
      display: flex;
      gap: 12px;
      align-items: flex-start;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.04);
      transition: var(--transition-fast);
    }

    .sat-card:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-hover);
      border-color: #cbd5e1;
    }

    .sat-icon-box {
      width: 36px;
      height: 36px;
      background: #FEF2F2;
      border: 1px solid #FEE2E2;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }

    .sat-info {
      flex: 1;
    }

    .sat-title-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 4px;
    }

    .sat-title {
      font-size: 0.74rem;
      font-weight: 800;
      letter-spacing: 0.03em;
      color: var(--text-main);
    }

    .sat-num {
      font-size: 0.65rem;
      font-weight: 700;
      color: var(--text-muted);
      background: #F1F5F9;
      padding: 1px 5px;
      border-radius: 4px;
    }

    .sat-desc {
      font-size: 0.68rem;
      color: var(--text-sub);
      line-height: 1.35;
    }

    /* Satellite Positions */
    .sat-top {
      top: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 250px;
    }
    .sat-top:hover {
      transform: translateX(-50%) translateY(-2px);
    }

    .sat-left {
      top: 360px;
      left: 0;
      width: 215px;
    }

    .sat-right {
      top: 360px;
      right: 0;
      width: 215px;
    }

    /* 3 SUBFEATURES (CIRCULAR LOGO PINNED EXACTLY ON DOTTED ORBIT LINE) */
    .subfeat-item {
      position: absolute;
      z-index: 5;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      text-align: center;
      gap: 6px;
      pointer-events: none;
    }

    .subfeat-circle {
      width: 38px;
      height: 38px;
      border-radius: 50%;
      background: #FEF2F2;
      border: 1.5px solid #FCA5A5;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 14px rgba(220, 38, 38, 0.12);
      color: var(--brand-red);
      pointer-events: auto;
      transition: all 0.2s ease;
    }

    .subfeat-text {
      font-size: 0.72rem;
      font-weight: 800;
      letter-spacing: 0.04em;
      line-height: 1.25;
      color: var(--brand-red);
      text-transform: uppercase;
      white-space: nowrap;
      pointer-events: auto;
    }

    .subfeat-item:hover .subfeat-circle {
      transform: scale(1.08);
      border-color: var(--brand-red);
      background: #FEE2E2;
      box-shadow: 0 6px 18px rgba(220, 38, 38, 0.24);
    }

    .badge-bot-defenses {
      left: 139px;
      top: 159px;
      transform: translate(-50%, -19px);
    }

    .badge-openapi {
      left: 601px;
      top: 159px;
      transform: translate(-50%, -19px);
    }

    .badge-token-auth {
      left: 370px;
      top: 440px;
      transform: translate(-50%, -19px);
    }

    /* LIVE DEMO SECTION */
    .live-demo-section {
      padding: 24px 0 60px 0;
      background: #FFFFFF;
      border-top: 1px solid var(--border-light);
    }

    .live-demo-section .section-tag-row {
      text-align: center;
      max-width: 920px;
      margin: 0 auto 24px auto;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .red-eyebrow {
      font-size: 0.82rem;
      font-weight: 800;
      color: var(--brand-red);
      letter-spacing: 0.06em;
      text-transform: uppercase;
      display: block;
      margin-bottom: 8px;
    }

    .main-section-title {
      font-size: 2.2rem;
      font-weight: 800;
      letter-spacing: -0.025em;
      color: var(--text-main);
      margin-bottom: 8px;
    }

    .main-section-sub {
      font-size: 1.05rem;
      color: var(--text-sub);
    }

    /* Interactive Demo Layout: 2 Big Centered Columns */
    .demo-interactive-grid {
      display: grid;
      grid-template-columns: 1fr 50px 1fr;
      gap: 28px;
      align-items: center;
      max-width: 1100px;
      width: 100%;
      margin: 0 auto;
      background: #FAFAFC;
      border: 1px solid var(--border-color);
      border-radius: 24px;
      padding: 28px 28px;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.04);
    }

    /* 1. Input Form Card */
    .demo-input-card {
      background: #FFFFFF;
      border: 1px solid var(--border-color);
      border-radius: 18px;
      padding: 36px 32px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.04);
      min-height: 420px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .input-field-group {
      margin-bottom: 18px;
    }

    .input-label {
      display: block;
      font-size: 0.92rem;
      font-weight: 700;
      color: #1E293B;
      margin-bottom: 8px;
    }

    .input-control {
      width: 100%;
      padding: 13px 16px;
      font-size: 1rem;
      font-family: inherit;
      border: 1px solid var(--border-color);
      border-radius: 12px;
      color: var(--text-main);
      background: #FFFFFF;
      transition: border-color 0.15s ease, box-shadow 0.15s ease;
    }

    .input-control:focus {
      outline: none;
      border-color: var(--brand-red);
      box-shadow: 0 0 0 3px var(--brand-red-glow);
    }

    .demo-submit-btn {
      margin-top: 14px;
      padding: 14px 28px;
      font-size: 1.02rem;
      font-weight: 700;
    }

    /* 2. Flow Indicator Arrow */
    .demo-flow-indicator {
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
    }

    .dotted-arrow-line {
      width: 44px;
      height: 2px;
      border-top: 2px dashed #EF4444;
    }

    .arrowhead-red {
      color: #EF4444;
      font-size: 0.85rem;
      margin-left: -3px;
      transform: scaleY(0.85);
    }

    /* 3. Output Card */
    .demo-output-card {
      background: #FFFFFF;
      border: 1px solid var(--border-color);
      border-radius: 18px;
      padding: 36px 32px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.04);
      min-height: 420px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      transition: var(--transition-fast);
    }

    .capture-badge-wrap {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--green-badge-bg);
      color: var(--green-badge-text);
      padding: 9px 18px;
      border-radius: var(--radius-pill);
      font-size: 0.95rem;
      font-weight: 700;
      margin-bottom: 24px;
    }

    .check-icon {
      font-weight: 800;
    }

    .capture-meta-table {
      display: flex;
      flex-direction: column;
      gap: 16px;
      margin-bottom: 24px;
    }

    .meta-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      font-size: 0.98rem;
      border-bottom: 1px solid var(--border-light);
      padding-bottom: 12px;
    }

    .meta-row:last-child {
      border-bottom: none;
      padding-bottom: 0;
    }

    .meta-col-label {
      display: flex;
      align-items: center;
      gap: 8px;
      color: var(--text-sub);
      font-weight: 500;
    }

    .meta-icon {
      font-size: 1.1rem;
    }

    .meta-col-value {
      font-weight: 600;
      color: var(--text-main);
    }

    .location-pill {
      background: #F1F5F9;
      padding: 5px 12px;
      border-radius: 8px;
      font-size: 0.9rem;
      font-weight: 600;
    }

    .status-received {
      color: #059669;
      background: #ECFDF5;
      padding: 4px 12px;
      border-radius: 8px;
      font-size: 0.88rem;
      font-weight: 700;
    }

    .font-code {
      font-family: var(--font-mono);
      font-size: 0.88rem;
      background: #F8FAFC;
      padding: 4px 10px;
      border-radius: 6px;
      color: #334155;
      letter-spacing: 0.02em;
    }

    .submission-footer-link {
      border-top: 1px solid var(--border-light);
      padding-top: 16px;
      text-align: center;
    }

    .btn-submission-view {
      font-size: 0.94rem;
      font-weight: 700;
      color: var(--text-main);
      display: inline-flex;
      align-items: center;
      gap: 6px;
      transition: color 0.15s ease;
    }

    .btn-submission-view:hover {
      color: var(--brand-red);
    }

    .demo-globe-card {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .globe-container {
      position: relative;
      width: 240px;
      height: 240px;
    }

    .globe-vector {
      width: 100%;
      height: 100%;
    }

    .globe-hub-center {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .pulse-glow-effect {
      position: absolute;
      width: 60px;
      height: 60px;
      background: radial-gradient(circle, rgba(225, 29, 72, 0.4) 0%, rgba(225, 29, 72, 0) 70%);
      border-radius: 50%;
      animation: pulse-ring 2.5s infinite ease-in-out;
    }

    .globe-logo-icon {
      width: 36px;
      height: 36px;
      background: var(--brand-red);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 14px rgba(225, 29, 72, 0.4);
      position: relative;
      z-index: 2;
    }

    @keyframes pulse-ring {
      0% { transform: scale(0.85); opacity: 0.6; }
      50% { transform: scale(1.3); opacity: 0.2; }
      100% { transform: scale(0.85); opacity: 0.6; }
    }

    .pulse-node {
      animation: blink-node 2s infinite ease-in-out;
    }

    @keyframes blink-node {
      0%, 100% { opacity: 0.4; }
      50% { opacity: 1; }
    }

    /* ONE PLATFORM SECTION */
    .one-platform-section {
      padding: 80px 0;
      background: #FFFFFF;
    }

    .platform-header-flex {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 40px;
    }

    .platform-title-block {
      max-width: 640px;
    }

    .platform-cards-row {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 24px;
    }

    .platform-feature-card {
      background: #FFFFFF;
      border: 1px solid var(--border-color);
      border-radius: var(--radius-card);
      padding: 28px 24px;
      display: flex;
      flex-direction: column;
      box-shadow: var(--shadow-card);
      transition: var(--transition-fast);
    }

    .platform-feature-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-hover);
      border-color: #cbd5e1;
    }

    .card-tag-pill {
      display: inline-block;
      align-self: flex-start;
      font-size: 0.72rem;
      font-weight: 800;
      letter-spacing: 0.05em;
      padding: 3px 8px;
      border-radius: 4px;
      margin-bottom: 16px;
    }

    .tag-blue { background: #EFF6FF; color: #2563EB; }
    .tag-red  { background: #FEF2F2; color: #DC2626; border: 1px solid #FCA5A5; }
    .tag-green { background: #ECFDF5; color: #059669; }

    .card-h3 {
      font-size: 1.35rem;
      font-weight: 800;
      color: var(--text-main);
      margin-bottom: 8px;
    }

    .card-p {
      font-size: 0.9rem;
      color: var(--text-sub);
      line-height: 1.5;
      margin-bottom: 24px;
      min-height: 42px;
    }

    .card-mini-mockup {
      background: #FAFAFC;
      border: 1px solid var(--border-color);
      border-radius: 10px;
      padding: 14px;
      margin-bottom: 20px;
      flex: 1;
      min-height: 150px;
      display: flex;
    }

    .mockup-forms {
      gap: 12px;
      align-items: center;
    }

    .mini-form-preview {
      width: 42%;
      background: #FFFFFF;
      border: 1px solid var(--border-color);
      border-radius: 6px;
      padding: 8px;
      display: flex;
      flex-direction: column;
      gap: 5px;
    }

    .m-bar { height: 6px; width: 60%; background: #E2E8F0; border-radius: 2px; }
    .m-input { height: 10px; width: 100%; background: #F8FAFC; border: 1px solid #E2E8F0; border-radius: 3px; }
    .m-textarea { height: 18px; width: 100%; background: #F8FAFC; border: 1px solid #E2E8F0; border-radius: 3px; }
    .m-btn { height: 10px; width: 40%; background: #94A3B8; border-radius: 3px; }

    .mini-code-preview {
      width: 58%;
      font-family: var(--font-mono);
      font-size: 0.62rem;
      color: #334155;
      line-height: 1.35;
    }

    .c-method { color: #2563EB; font-weight: 700; }
    .c-header { color: #64748B; }
    .c-key { color: #DC2626; }
    .c-val { color: #059669; }

    .mockup-mail {
      flex-direction: column;
    }

    .mail-inbox-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: #FFFFFF;
      border: 1px solid var(--border-color);
      border-radius: 6px;
      padding: 6px 8px;
      font-size: 0.72rem;
      margin-bottom: 8px;
    }

    .mail-inbox-address {
      font-weight: 600;
      color: var(--text-main);
    }

    .mail-inbox-count {
      background: #F1F5F9;
      padding: 1px 6px;
      border-radius: 4px;
      font-size: 0.65rem;
      font-weight: 700;
    }

    .mail-inbox-items {
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    .mail-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: #FFFFFF;
      border: 1px solid var(--border-light);
      border-radius: 6px;
      padding: 5px 8px;
      font-size: 0.68rem;
    }

    .mail-item-left {
      display: flex;
      align-items: center;
      gap: 5px;
    }

    .mail-item-title {
      font-weight: 500;
      color: var(--text-main);
    }

    .mail-item-time {
      color: var(--text-light);
      font-size: 0.62rem;
    }

    .mockup-test {
      flex-direction: column;
    }

    .test-inbox-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      font-size: 0.72rem;
      margin-bottom: 8px;
      padding-bottom: 6px;
      border-bottom: 1px solid var(--border-light);
    }

    .test-copy-btn {
      background: #FFFFFF;
      border: 1px solid var(--border-color);
      border-radius: 4px;
      font-size: 0.65rem;
      padding: 2px 8px;
      font-weight: 600;
      color: var(--text-sub);
    }

    .test-inbox-items {
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    .test-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: #FFFFFF;
      border: 1px solid var(--border-light);
      border-radius: 6px;
      padding: 5px 8px;
      font-size: 0.68rem;
    }

    .badge-status-success {
      background: #ECFDF5;
      color: #059669;
      padding: 1px 6px;
      border-radius: 4px;
      font-size: 0.62rem;
      font-weight: 700;
    }

    .card-action-link {
      font-size: 0.85rem;
      font-weight: 700;
      display: inline-flex;
      align-items: center;
      gap: 4px;
      transition: transform 0.15s ease;
    }

    .card-action-link:hover {
      transform: translateX(4px);
    }

    .link-blue { color: #2563EB; }
    .link-red  { color: #E11D48; }
    .link-green { color: #059669; }

    /* TRUSTED BY BUILDERS */
    .trusted-builders-section {
      padding: 48px 0 64px 0;
      border-top: 1px solid var(--border-light);
      border-bottom: 1px solid var(--border-light);
      background: #FFFFFF;
      text-align: center;
    }

    .trusted-headline {
      font-size: 0.75rem;
      font-weight: 700;
      letter-spacing: 0.08em;
      color: var(--text-light);
      margin-bottom: 28px;
    }

    .trusted-logos-flex {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 48px;
      flex-wrap: wrap;
    }

    .builder-logo {
      display: flex;
      align-items: center;
      gap: 8px;
      color: #475569;
      font-size: 1.05rem;
      font-weight: 700;
      opacity: 0.85;
      transition: opacity 0.2s ease;
    }

    .builder-logo:hover {
      opacity: 1;
      color: var(--text-main);
    }

    /* INTERACTIVE CONSOLE PLAYGROUND */
    .api-console-section {
      padding: 80px 0;
      background: #FAFAFC;
      border-bottom: 1px solid var(--border-color);
    }

    .console-box {
      background: #0F172A;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.2);
    }

    .console-tab-bar {
      display: flex;
      align-items: center;
      background: #1E293B;
      padding: 6px 12px;
      gap: 6px;
      border-bottom: 1px solid #334155;
    }

    .c-tab-btn {
      padding: 8px 16px;
      border-radius: 6px;
      font-size: 0.82rem;
      font-weight: 600;
      color: #94A3B8;
      transition: all 0.15s ease;
    }

    .c-tab-btn.active {
      background: #0F172A;
      color: #FFFFFF;
    }

    .c-openapi-link {
      margin-left: auto;
      font-size: 0.78rem;
      color: #94A3B8;
      padding: 4px 10px;
    }
    .c-openapi-link:hover {
      color: #FFFFFF;
    }

    .c-tab-panel {
      display: none;
      padding: 24px;
    }

    .c-tab-panel.active {
      display: block;
    }

    .terminal-code {
      font-family: var(--font-mono);
      font-size: 0.88rem;
      color: #E2E8F0;
      line-height: 1.6;
      white-space: pre-wrap;
      margin-bottom: 16px;
    }

    .c-prompt { color: #64748B; }
    .c-cmd { color: #38BDF8; font-weight: 600; }
    .c-str { color: #A7F3D0; }
    .c-hl { color: #DC2626; font-weight: 600; }
    .c-muted { color: #64748B; font-style: italic; }
    .c-ok { color: #4ADE80; }

    .terminal-bar {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 16px;
      padding-top: 12px;
      border-top: 1px solid #1E293B;
    }

    .btn-accent-red {
      background: var(--brand-red);
      color: #FFFFFF;
      padding: 6px 14px;
      font-size: 0.82rem;
      border-radius: 6px;
    }

    .btn-accent-red:hover {
      background: var(--brand-red-hover);
    }

    .btn-ghost-dark {
      background: #1E293B;
      color: #E2E8F0;
      border: 1px solid #334155;
      padding: 6px 14px;
      font-size: 0.82rem;
      border-radius: 6px;
    }

    .btn-ghost-dark:hover {
      background: #334155;
    }

    .terminal-note {
      font-size: 0.75rem;
      color: #64748B;
    }

    .terminal-response {
      background: #090E1A;
      border: 1px solid #1E293B;
      border-radius: 8px;
      padding: 14px;
      font-family: var(--font-mono);
      font-size: 0.82rem;
      color: #A7F3D0;
      line-height: 1.5;
      white-space: pre-wrap;
      min-height: 52px;
    }

    /* CODE INTEGRATIONS */
    .code-integrations-section {
      padding: 80px 0;
      background: #FFFFFF;
    }

    .code-tabs-box {
      background: #0F172A;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    }

    .code-tabs-header {
      display: flex;
      background: #1E293B;
      padding: 6px 12px;
      gap: 4px;
      border-bottom: 1px solid #334155;
    }

    .code-tab-nav {
      padding: 8px 16px;
      border-radius: 6px;
      font-size: 0.82rem;
      font-weight: 600;
      color: #94A3B8;
      transition: all 0.15s ease;
    }

    .code-tab-nav.active {
      background: #0F172A;
      color: #FFFFFF;
    }

    .code-tab-content {
      display: none;
      padding: 24px;
    }

    .code-tab-content.active {
      display: block;
    }

    .code-snippet {
      font-family: var(--font-mono);
      font-size: 0.88rem;
      color: #E2E8F0;
      line-height: 1.6;
      white-space: pre-wrap;
    }

    /* ONBOARDING & FEATURES */
    .onboarding-section, .core-features-section {
      padding: 80px 0;
      background: #FAFAFC;
      border-top: 1px solid var(--border-light);
    }

    .steps-grid-3 {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 24px;
    }

    .step-card {
      background: #FFFFFF;
      border: 1px solid var(--border-color);
      border-radius: var(--radius-card);
      padding: 32px 24px;
      box-shadow: var(--shadow-card);
      position: relative;
      transition: var(--transition-fast);
    }

    .step-card:hover {
      transform: translateY(-3px);
      box-shadow: var(--shadow-hover);
    }

    .step-badge-num {
      width: 36px;
      height: 36px;
      background: #F1F5F9;
      color: var(--text-main);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 800;
      font-size: 0.95rem;
      margin-bottom: 16px;
    }

    .step-card-featured .step-badge-num {
      background: #FEF2F2;
      color: var(--brand-red);
      border: 1.5px solid #FCA5A5;
    }

    .step-eyebrow {
      font-size: 0.75rem;
      font-weight: 700;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      display: block;
      margin-bottom: 8px;
    }

    .step-title {
      font-size: 1.25rem;
      font-weight: 800;
      color: var(--text-main);
      margin-bottom: 10px;
    }

    .step-desc {
      font-size: 0.88rem;
      color: var(--text-sub);
      line-height: 1.5;
      margin-bottom: 20px;
    }

    .step-link {
      font-size: 0.82rem;
      font-weight: 700;
      color: var(--text-main);
    }

    .step-link:hover {
      color: var(--brand-red);
    }

    .features-list-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }

    .feature-item-row {
      background: #FFFFFF;
      border: 1px solid var(--border-color);
      border-radius: 12px;
      padding: 24px;
      box-shadow: var(--shadow-card);
    }

    .feat-title {
      font-size: 1.05rem;
      font-weight: 800;
      color: var(--text-main);
      margin-bottom: 8px;
    }

    .feat-desc {
      font-size: 0.88rem;
      color: var(--text-sub);
      line-height: 1.55;
    }

    /* PRICING */
    .pricing-section {
      padding: 80px 0;
      background: #FFFFFF;
      border-top: 1px solid var(--border-light);
    }

    .pricing-cards-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 24px;
      align-items: stretch;
    }

    .pricing-plan-card {
      background: #FFFFFF;
      border: 1px solid var(--border-color);
      border-radius: 20px;
      padding: 36px 28px;
      display: flex;
      flex-direction: column;
      box-shadow: var(--shadow-card);
      position: relative;
    }

    .featured-plan {
      border: 2px solid var(--brand-red);
      box-shadow: 0 12px 36px rgba(220, 38, 38, 0.12);
    }

    .popular-ribbon {
      position: absolute;
      top: -12px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--brand-red);
      color: #FFFFFF;
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      padding: 3px 12px;
      border-radius: var(--radius-pill);
    }

    .plan-head {
      margin-bottom: 24px;
    }

    .plan-name {
      font-size: 1.4rem;
      font-weight: 800;
      color: var(--text-main);
      margin-bottom: 4px;
    }

    .plan-desc {
      font-size: 0.85rem;
      color: var(--text-muted);
      margin-bottom: 20px;
    }

    .plan-price {
      display: flex;
      align-items: baseline;
      gap: 4px;
    }

    .plan-price .currency {
      font-size: 1.5rem;
      font-weight: 800;
      color: var(--text-main);
    }

    .plan-price .amount {
      font-size: 2.8rem;
      font-weight: 800;
      color: var(--text-main);
      line-height: 1;
    }

    .plan-price .period {
      font-size: 0.88rem;
      color: var(--text-muted);
    }

    .plan-features {
      display: flex;
      flex-direction: column;
      gap: 12px;
      font-size: 0.88rem;
      color: var(--text-sub);
      margin-bottom: 32px;
      flex: 1;
    }

    /* CTA & FOOTER */
    .cta-bottom-section {
      padding: 64px 0 80px 0;
    }

    .cta-inner-box {
      background: #09090B;
      border-radius: 24px;
      padding: 64px 32px;
      text-align: center;
      color: #FFFFFF;
    }

    .cta-heading {
      font-size: 2.2rem;
      font-weight: 800;
      letter-spacing: -0.025em;
      margin-bottom: 12px;
    }

    .cta-sub {
      font-size: 1.05rem;
      color: #94A3B8;
      margin-bottom: 32px;
    }

    .cta-buttons-wrap {
      display: flex;
      justify-content: center;
      gap: 14px;
    }

    .site-footer {
      border-top: 1px solid var(--border-color);
      padding: 64px 0 40px 0;
      background: #FFFFFF;
    }

    .footer-layout {
      display: grid;
      grid-template-columns: 2fr 1fr 1fr 1fr;
      gap: 48px;
    }

    .footer-tagline {
      font-size: 0.88rem;
      color: var(--text-sub);
      margin: 16px 0 20px 0;
      max-width: 320px;
      line-height: 1.5;
    }

    .footer-copyright {
      font-size: 0.78rem;
      color: var(--text-light);
    }

    .footer-col-title {
      font-size: 0.85rem;
      font-weight: 700;
      color: var(--text-main);
      margin-bottom: 16px;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .footer-links {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .footer-links a {
      font-size: 0.88rem;
      color: var(--text-sub);
      transition: color 0.15s ease;
    }

    .footer-links a:hover {
      color: var(--brand-red);
    }

    @media (max-width: 1200px) {
      .hero-layout {
        grid-template-columns: 1fr;
        gap: 48px;
      }
      .hero-left {
        max-width: 100%;
        text-align: center;
      }
      .hero-tag-badge, .hero-cta-buttons, .hero-geo-callout {
        justify-content: center;
        margin-left: auto;
        margin-right: auto;
      }
      .diagram-hub-wrapper {
        margin: 0 auto;
        transform: scale(0.9);
      }
      .demo-interactive-grid {
        grid-template-columns: 1fr;
        gap: 24px;
      }
      .demo-flow-indicator {
        transform: rotate(90deg);
        padding: 10px 0;
      }
      .platform-cards-row, .steps-grid-3, .pricing-cards-grid {
        grid-template-columns: 1fr;
      }
      .features-list-grid {
        grid-template-columns: 1fr;
      }
      .footer-layout {
        grid-template-columns: 1fr 1fr;
        gap: 32px;
      }
    }

    @media (max-width: 768px) {
      .nav-pill, .nav-signin {
        display: none;
      }
      .nav-burger {
        display: block;
      }
      .nav-mobile.open {
        display: flex;
        flex-direction: column;
        position: absolute;
        top: 100%;
        left: 0;
        width: 100%;
        background: #FFFFFF;
        border-bottom: 1px solid var(--border-color);
        padding: 20px;
        gap: 16px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      }
      .hero-main-title {
        font-size: 2.3rem;
      }
      .diagram-hub-wrapper {
        width: 100%;
        max-width: 500px;
        height: 440px;
        transform: scale(0.7);
        transform-origin: top center;
      }
      .footer-layout {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>

  <!-- ============ TOP ANNOUNCEMENT BAR ============ -->
  <aside class="announce-bar">
    <div class="container announce-inner">
      <span class="announce-dot" aria-hidden="true">🔴</span>
      <span class="announce-text"><strong>GEO-smart leads</strong> — every form submission auto-stamped with country, city, region, and timezone.</span>
    </div>
  </aside>

  <!-- ============ NAVBAR ============ -->
  <header class="navbar" id="navbar">
    <div class="container nav-inner">
      <a href="/" class="logo-lockup" aria-label="Ollastack — Built for AI Agents">
        <img src="/logo.svg" alt="Ollastack — Built for AI Agents" class="nav-brand-logo" height="48" />
      </a>

      <nav class="nav-pill" aria-label="Primary">
        <div class="nav-dropdown-wrap">
          <a href="#product" class="nav-link">Product <span class="chevron">▾</span></a>
        </div>
        <a href="/use-cases" class="nav-link">Use cases</a>
        <a href="/email-api" class="nav-link">Email API</a>
        <a href="/docs" class="nav-link">Docs</a>
        <div class="nav-dropdown-wrap">
          <a href="/resources" class="nav-link">Resources <span class="chevron">▾</span></a>
        </div>
        <a href="/blog" class="nav-link">Blog</a>
        <a href="/pricing" class="nav-link">Pricing</a>
      </nav>

      <div class="nav-right">
        <a href={`${APP}/login`} class="nav-signin">Sign in</a>
        <a href={`${APP}/register`} class="btn btn-dark-pill">
          Start free <span class="arrow">→</span>
        </a>
      </div>

      <button class="nav-burger" id="nav-burger" aria-label="Open menu" aria-expanded="false">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 7h16M4 12h16M4 17h16" stroke-linecap="round"/></svg>
      </button>
    </div>

    <!-- Mobile Navigation Drawer -->
    <nav class="nav-mobile" id="nav-mobile" aria-label="Mobile">
      <a href="#product" class="nav-mobile-link active">Product</a>
      <a href="/use-cases" class="nav-mobile-link">Use cases</a>
      <a href="/email-api" class="nav-mobile-link">Email API</a>
      <a href="/docs" class="nav-mobile-link">Docs</a>
      <a href="/resources" class="nav-mobile-link">Resources</a>
      <a href="/blog" class="nav-mobile-link">Blog</a>
      <a href="/pricing" class="nav-mobile-link">Pricing</a>
      <div class="nav-mobile-actions">
        <a href={`${APP}/login`} class="nav-mobile-link">Sign in</a>
        <a href={`${APP}/register`} class="btn btn-dark-pill nav-mobile-cta">Start free →</a>
      </div>
    </nav>
  </header>

  <main>
    <!-- ============ HERO SECTION ============ -->
    <section class="hero-section">
      <div class="container hero-layout">
        <div class="hero-content">
          <div class="hero-tag-badge">
            <span class="badge-feature-item"><span class="badge-blue-dot"></span> API-FIRST</span>
            <span class="badge-feature-item"><span class="badge-blue-dot"></span> AGENT FORMS</span>
            <span class="badge-feature-item"><span class="badge-blue-dot"></span> MAILBOXES</span>
          </div>

          <h1 class="hero-main-title">
            <span class="nowrap-headline">Forms, mailboxes &amp; email testing,</span><br>
            Built for your<br>
            <span class="text-brand-red">AI agents.</span>
          </h1>

          <p class="hero-description">
            Three things your agents need, one backend: form endpoints they POST to and sail past the bot defenses, real mailboxes they send, receive, reply, and read OTP codes from, and disposable inboxes for testing email in CI. One token system, one OpenAPI spec they read themselves.
          </p>

          <div class="hero-cta-buttons">
            <a href={`${APP}/register`} class="btn btn-dark-pill btn-hero">
              Start free <span class="arrow">→</span>
            </a>
            <a href="/docs" class="btn btn-white-pill btn-hero">
              Read the docs
            </a>
          </div>

          <div class="hero-geo-callout">
            <span class="geo-pin" aria-hidden="true">📍</span>
            <p class="geo-text"><strong>GEO-smart leads</strong> — every form submission auto-stamped with country, city, region, and timezone.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- ============ LIVE DEMO SECTION ============ -->
    <section class="live-demo-section" id="live-demo">
      <div class="container">
        <div class="section-tag-row">
          <span class="red-eyebrow">/ LIVE DEMO</span>
          <h2 class="main-section-title">See Ollastack in action.</h2>
          <p class="main-section-sub">Submit a form and watch what happens.</p>
        </div>

        <div class="demo-interactive-grid">
          <!-- 1. Input Form Mockup -->
          <div class="demo-input-card">
            <form id="hero-interactive-form">
              <div class="input-field-group">
                <label for="demo-input-name" class="input-label">Name</label>
                <input type="text" id="demo-input-name" class="input-control" value="Ada Lovelace" required autocomplete="name" />
              </div>
              <div class="input-field-group">
                <label for="demo-input-email" class="input-label">Email</label>
                <input type="email" id="demo-input-email" class="input-control" value="ada@example.com" required autocomplete="email" />
              </div>
              <div class="input-field-group">
                <label for="demo-input-msg" class="input-label">Message</label>
                <input type="text" id="demo-input-msg" class="input-control" value="Hello from my AI agent!" required />
              </div>
              <button type="submit" class="btn btn-dark-pill btn-full demo-submit-btn" id="demo-submit-button">
                Submit demo <span class="arrow">→</span>
              </button>
            </form>
          </div>

          <!-- 2. Animated Flow Connector Arrow -->
          <div class="demo-flow-indicator" aria-hidden="true">
            <div class="dotted-arrow-line"></div>
            <div class="arrowhead-red">▶</div>
          </div>

          <!-- 3. Output Result Card -->
          <div class="demo-output-card" id="demo-output-display">
            <div class="capture-badge-wrap">
              <span class="check-icon">✓</span>
              <span class="capture-text">Submission captured!</span>
            </div>

            <div class="capture-meta-table">
              <div class="meta-row">
                <div class="meta-col-label">
                  <span class="meta-icon">📍</span>
                  <span>Location</span>
                </div>
                <div class="meta-col-value">
                  <span class="location-pill" id="live-loc-value">Bengaluru, India 🇮🇳</span>
                </div>
              </div>

              <div class="meta-row">
                <div class="meta-col-label">
                  <span class="meta-icon">🕒</span>
                  <span>Time</span>
                </div>
                <div class="meta-col-value" id="live-time-value">
                  May 18, 2025, 12:41 PM IST
                </div>
              </div>

              <div class="meta-row">
                <div class="meta-col-label">
                  <span class="meta-icon">🔘</span>
                  <span>Status</span>
                </div>
                <div class="meta-col-value">
                  <span class="status-received">● Received</span>
                </div>
              </div>

              <div class="meta-row">
                <div class="meta-col-label">
                  <span class="meta-icon">🆔</span>
                  <span>Lead ID</span>
                </div>
                <div class="meta-col-value font-code" id="live-lead-id">
                  lead_01JXBZQ1KSK6Y2M7T9R1
                </div>
              </div>
            </div>

            <div class="submission-footer-link">
              <a href="#full-submission" class="btn-submission-view">
                View full submission <span class="arrow">→</span>
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ============ ONE PLATFORM SECTION ============ -->
    <section class="one-platform-section" id="product">
      <div class="container">
        <div class="platform-header-flex">
          <div class="platform-title-block">
            <span class="red-eyebrow">/ ONE PLATFORM</span>
            <h2 class="main-section-title">Everything your agents can do.</h2>
            <p class="main-section-sub">Forms in, mail out — one backend, one token system, one console. Each surface is in the OpenAPI spec your agent reads itself.</p>
          </div>
          <div class="platform-header-action">
            <a href="/docs" class="btn btn-white-pill">
              Explore all docs <span class="arrow">→</span>
            </a>
          </div>
        </div>

        <div class="platform-cards-row">
          <!-- Card 1: Headless forms -->
          <div class="platform-feature-card card-forms">
            <div class="card-tag-pill tag-blue">01 FORMS</div>
            <h3 class="card-h3">Headless forms</h3>
            <p class="card-p">One endpoint. Every submission GEO-stamped, spam-filtered, and agent-aware.</p>
            
            <div class="card-mini-mockup mockup-forms">
              <div class="mini-form-preview">
                <div class="m-bar"></div>
                <div class="m-input"></div>
                <div class="m-input"></div>
                <div class="m-textarea"></div>
                <div class="m-btn"></div>
              </div>
              <div class="mini-code-preview">
                <div class="code-line"><span class="c-method">POST</span> /v1/forms/{id}/submit</div>
                <div class="code-line"><span class="c-header">Authorization:</span> Bearer {token}</div>
                <div class="code-line"><span class="c-header">Content-Type:</span> application/json</div>
                <div class="code-json">
                  <div>{</div>
                  <div>&nbsp;&nbsp;<span class="c-key">"name"</span>: <span class="c-val">"Ada"</span>,</div>
                  <div>&nbsp;&nbsp;<span class="c-key">"email"</span>: <span class="c-val">"ada@example.com"</span></div>
                  <div>}</div>
                </div>
              </div>
            </div>
            
            <a href="#live-demo" class="card-action-link link-blue">→ Call it live</a>
          </div>

          <!-- Card 2: Agent Mail -->
          <div class="platform-feature-card card-mail">
            <div class="card-tag-pill tag-red">02 AGENT MAIL</div>
            <h3 class="card-h3">Agent Mail</h3>
            <p class="card-p">Real email inboxes your agents own.</p>
            
            <div class="card-mini-mockup mockup-mail">
              <div class="mail-inbox-header">
                <span class="mail-inbox-icon">✉</span>
                <span class="mail-inbox-address">support@agent.ollastack.com</span>
                <span class="mail-inbox-count">3</span>
              </div>
              <div class="mail-inbox-items">
                <div class="mail-item">
                  <div class="mail-item-left">
                    <span class="mail-item-icon">✉</span>
                    <span class="mail-item-title">New message from user</span>
                  </div>
                  <span class="mail-item-time">2m ago</span>
                </div>
                <div class="mail-item">
                  <div class="mail-item-left">
                    <span class="mail-item-icon">✉</span>
                    <span class="mail-item-title">OTP is 364921</span>
                  </div>
                  <span class="mail-item-time">5m ago</span>
                </div>
                <div class="mail-item">
                  <div class="mail-item-left">
                    <span class="mail-item-icon">✉</span>
                    <span class="mail-item-title">Welcome to Ollastack</span>
                  </div>
                  <span class="mail-item-time">12m ago</span>
                </div>
              </div>
            </div>
            
            <a href="/docs/agent-mail" class="card-action-link link-red">→ View docs</a>
          </div>

          <!-- Card 3: Test Inboxes -->
          <div class="platform-feature-card card-test">
            <div class="card-tag-pill tag-green">03 TEST INBOXES</div>
            <h3 class="card-h3">Mail testing for CI</h3>
            <p class="card-p">Disposable inboxes on @test.ollastack.com: read OTPs, assert, and test every email.</p>
            
            <div class="card-mini-mockup mockup-test">
              <div class="test-inbox-header">
                <span>Inbox: <strong>test_01HXY2</strong></span>
                <button class="test-copy-btn" id="btn-copy-test-inbox">Copy</button>
              </div>
              <div class="test-inbox-items">
                <div class="test-item">
                  <span>OTP detected: <strong>364921</strong></span>
                  <span class="badge-status-success">Success</span>
                </div>
                <div class="test-item">
                  <span>Code extracted</span>
                  <span class="badge-status-success">Success</span>
                </div>
                <div class="test-item">
                  <span>Assertion passed</span>
                  <span class="badge-status-success">Success</span>
                </div>
              </div>
            </div>
            
            <a href="/docs/api" class="card-action-link link-green">→ View docs</a>
          </div>
        </div>
      </div>
    </section>

    <!-- ============ TRUSTED BY BUILDERS ============ -->
    <section class="trusted-builders-section">
      <div class="container">
        <h4 class="trusted-headline">TRUSTED BY BUILDERS</h4>
        <div class="trusted-logos-flex">
          <div class="builder-logo">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 2l8.66 5v10L12 22l-8.66-5V7L12 2z"/>
              <circle cx="12" cy="12" r="3"/>
            </svg>
            <span>Hexmos</span>
          </div>
          <div class="builder-logo">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/>
              <circle cx="9" cy="12" r="1" fill="currentColor"/>
              <circle cx="15" cy="12" r="1" fill="currentColor"/>
            </svg>
            <span>ChatNode</span>
          </div>
          <div class="builder-logo">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="3" y="3" width="7" height="7" rx="1"/>
              <rect x="14" y="3" width="7" height="7" rx="1"/>
              <rect x="14" y="14" width="7" height="7" rx="1"/>
              <path d="M10 6.5h4M17.5 10v4"/>
            </svg>
            <span>FlowBase</span>
          </div>
          <div class="builder-logo">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="4 17 10 11 4 5"/>
              <line x1="12" y1="19" x2="20" y2="19"/>
            </svg>
            <span>CodePilot</span>
          </div>
          <div class="builder-logo">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="9"/>
              <path d="M12 3v18M3 12h18"/>
            </svg>
            <span>SynthAI</span>
          </div>
          <div class="builder-logo">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="4" y="4" width="16" height="16" rx="4"/>
              <circle cx="9" cy="10" r="1.5" fill="currentColor"/>
              <circle cx="15" cy="10" r="1.5" fill="currentColor"/>
              <path d="M9.5 15a3.5 3.5 0 0 0 5 0"/>
            </svg>
            <span>BuildBear</span>
          </div>
        </div>
      </div>
    </section>

    <!-- ============ INTERACTIVE CONSOLE PLAYGROUND ============ -->
    <section class="api-console-section" id="api-console">
      <div class="container">
        <div class="section-tag-row">
          <span class="red-eyebrow">/ API-FIRST · CALL IT NOW</span>
          <h2 class="main-section-title">Three capabilities. One API. Call them live.</h2>
          <p class="main-section-sub">The <strong>Forms</strong> and <strong>Test-inbox</strong> demos run for real, right here — no key, no signup.</p>
        </div>

        <div class="console-box">
          <div class="console-tab-bar" role="tablist">
            <button class="c-tab-btn active" data-tab="forms" role="tab" aria-selected="true">📨 Forms</button>
            <button class="c-tab-btn" data-tab="mail" role="tab" aria-selected="false">🤖 Agent Mail</button>
            <button class="c-tab-btn" data-tab="test" role="tab" aria-selected="false">✅ Test Inboxes</button>
            <a class="c-openapi-link" href={`${APP}/api/openapi.json`} target="_blank" rel="noopener">openapi.json ↗</a>
          </div>

          <!-- Tab 1: Forms -->
          <div class="c-tab-panel active" data-tab="forms">
            <pre class="terminal-code"><span class="c-prompt">$</span> <span class="c-cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="c-hl">contact</span> \
    -H <span class="c-str">"Content-Type: application/json"</span> \
    -d <span class="c-str">'{"name":"Ada","email":"ada@example.com","message":"Tell me more"}'</span></pre>
            <div class="terminal-bar">
              <button class="btn btn-accent-red btn-sm" id="run-forms-btn">▶ Run it live</button>
              <button class="btn btn-ghost-dark btn-sm" id="copy-forms-btn">Copy</button>
              <span class="terminal-note">live sandbox · no token needed</span>
            </div>
            <pre class="terminal-response" id="res-forms-output"><span class="c-muted"># the response shows here — geo is auto-stamped server-side</span></pre>
          </div>

          <!-- Tab 2: Mail -->
          <div class="c-tab-panel" data-tab="mail">
            <pre class="terminal-code"><span class="c-muted"># claim your agent's identity, then send as it</span>
<span class="c-prompt">$</span> <span class="c-cmd">curl</span> -X POST https://login.ollastack.com/api/mailboxes \
    -H <span class="c-str">"Authorization: Bearer fmd_&lt;token&gt;"</span> \
    -d <span class="c-str">'{"name":"Support","mode":"agent","handle":"support"}'</span>
<span class="c-prompt">$</span> <span class="c-cmd">curl</span> -X POST https://login.ollastack.com/api/mailboxes/$ID/<span class="c-hl">send</span> \
    -d <span class="c-str">'{"to":"user@example.com","subject":"Re: your demo","text":"…"}'</span></pre>
            <div class="terminal-bar">
              <button class="btn btn-ghost-dark btn-sm">Copy</button>
              <span class="terminal-note">needs a mail token · runs in your terminal</span>
            </div>
            <pre class="terminal-response"><span class="c-ok">{"success": true, "data": {
  "address": "support@agent.ollastack.com",
  "direction": "outbound", "deliveryStatus": "sent"}}</span></pre>
          </div>

          <!-- Tab 3: Test -->
          <div class="c-tab-panel" data-tab="test">
            <pre class="terminal-code"><span class="c-muted"># disposable inbox → wait for the email → read the OTP</span>
<span class="c-prompt">$</span> <span class="c-cmd">curl</span> -X POST https://login.ollastack.com/api/mailboxes \
    -H <span class="c-str">"Authorization: Bearer fmd_&lt;token&gt;"</span> \
    -d <span class="c-str">'{"name":"signup-ci","mode":"test"}'</span>
<span class="c-prompt">$</span> <span class="c-cmd">curl</span> <span class="c-str">"https://login.ollastack.com/api/mailboxes/$ID/<span class="c-hl">wait</span>?timeout=55"</span> \
    -H <span class="c-str">"Authorization: Bearer fmd_&lt;token&gt;"</span></pre>
            <div class="terminal-bar">
              <button class="btn btn-accent-red btn-sm" id="run-test-btn">▶ Run it live</button>
              <button class="btn btn-ghost-dark btn-sm">Copy</button>
              <span class="terminal-note">live · no token needed</span>
            </div>
            <pre class="terminal-response" id="res-test-output"><span class="c-muted"># click Run — extracts the OTP, exactly like /wait + codes[0] in CI</span></pre>
          </div>
        </div>
      </div>
    </section>

    <!-- ============ CODE INTEGRATIONS ============ -->
    <section class="code-integrations-section">
      <div class="container">
        <div class="section-tag-row">
          <span class="red-eyebrow">/ DROP-IN CODE</span>
          <h2 class="main-section-title">One endpoint. Five ways to call it.</h2>
          <p class="main-section-sub">Same URL serves your AI agent, your static HTML form, your React app, your mobile client.</p>
        </div>

        <div class="code-tabs-box">
          <div class="code-tabs-header">
            <button class="code-tab-nav active" data-lang="bearer">Agent (Bearer) 🤖</button>
            <button class="code-tab-nav" data-lang="curl">cURL</button>
            <button class="code-tab-nav" data-lang="js">fetch</button>
            <button class="code-tab-nav" data-lang="html">HTML</button>
            <button class="code-tab-nav" data-lang="react">React</button>
          </div>
          <div class="code-tab-content active" data-lang="bearer">
            <pre class="code-snippet"><span class="c-prompt">$</span> <span class="c-cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="c-hl">contact</span> \
    -H <span class="c-str">"Authorization: Bearer fmd_&lt;your-agent-token&gt;"</span> \
    -H <span class="c-str">"Content-Type: application/json"</span> \
    -d <span class="c-str">'{"name":"Ada","message":"hi"}'</span>

<span class="c-ok">{"success": true, "id": "sub_8f2…", "country": "IN", "city": "Bengaluru"}</span>
<span class="c-muted"># tagged in the inbox with the agent's label
# every call recorded in the audit log</span></pre>
          </div>
          <div class="code-tab-content" data-lang="curl">
            <pre class="code-snippet"><span class="c-prompt">$</span> <span class="c-cmd">curl</span> -X POST https://login.ollastack.com/api/submit/<span class="c-hl">contact</span> \
    -H <span class="c-str">"Content-Type: application/json"</span> \
    -d <span class="c-str">'{"name":"Ada","message":"hi"}'</span>

<span class="c-ok">{"success": true, "id": "sub_8f2…"}</span></pre>
          </div>
          <div class="code-tab-content" data-lang="js">
            <pre class="code-snippet"><span class="c-key">await</span> fetch(<span class="c-str">"https://login.ollastack.com/api/submit/contact"</span>, {
  method: <span class="c-str">"POST"</span>,
  headers: { <span class="c-str">"Content-Type"</span>: <span class="c-str">"application/json"</span> },
  body: JSON.stringify({ name, email, message }),
});</pre>
          </div>
          <div class="code-tab-content" data-lang="html">
            <pre class="code-snippet"><span class="c-tag">&lt;form</span> action=<span class="c-str">"https://login.ollastack.com/api/submit/contact"</span> method=<span class="c-str">"POST"</span><span class="c-tag">&gt;</span>
  <span class="c-tag">&lt;input</span> name=<span class="c-str">"email"</span> type=<span class="c-str">"email"</span> required<span class="c-tag">/&gt;</span>
  <span class="c-tag">&lt;textarea</span> name=<span class="c-str">"message"</span> required<span class="c-tag">&gt;&lt;/textarea&gt;</span>
  <span class="c-tag">&lt;button&gt;</span>Send<span class="c-tag">&lt;/button&gt;</span>
<span class="c-tag">&lt;/form&gt;</span></pre>
          </div>
          <div class="code-tab-content" data-lang="react">
            <pre class="code-snippet"><span class="c-key">import</span> { useForm } <span class="c-key">from</span> <span class="c-str">"@ollastack/react"</span>;

<span class="c-key">export function</span> <span class="c-fn">Contact</span>() {
  <span class="c-key">const</span> [state, handleSubmit] = <span class="c-fn">useForm</span>(<span class="c-str">"contact"</span>);
  <span class="c-key">if</span> (state.succeeded) <span class="c-key">return</span> <span class="c-tag">&lt;p&gt;</span>Thanks!<span class="c-tag">&lt;/p&gt;</span>;
  <span class="c-key">return</span> (
    <span class="c-tag">&lt;form onSubmit={handleSubmit}&gt;</span>...<span class="c-tag">&lt;/form&gt;</span>
  );
}</pre>
          </div>
        </div>
      </div>
    </section>

    <!-- ============ ONBOARDING STEPS ============ -->
    <section class="onboarding-section">
      <div class="container">
        <div class="section-tag-row">
          <span class="red-eyebrow">/ TWO-MINUTE ONBOARDING</span>
          <h2 class="main-section-title">From API key to your first agent submission in two minutes.</h2>
          <p class="main-section-sub">Three small steps. One curl block. Your agent is sending real submissions before your coffee cools.</p>
        </div>
        <div class="steps-grid-3">
          <div class="step-card">
            <div class="step-badge-num">1</div>
            <span class="step-eyebrow">step 1 · 30 seconds</span>
            <h3 class="step-title">Create a form</h3>
            <p class="step-desc">Sign up, click <strong>Create form</strong>, name it. Your endpoint URL is yours immediately.</p>
            <a class="step-link" href={`${APP}/register`}>→ create your first form</a>
          </div>
          <div class="step-card">
            <div class="step-badge-num">2</div>
            <span class="step-eyebrow">step 2 · 60 seconds</span>
            <h3 class="step-title">Connect your agent</h3>
            <p class="step-desc">One click mints a scoped, audited token and hands you a paste-ready agent prompt.</p>
            <a class="step-link" href={`${APP}/dashboard/connect`}>→ connect your agent</a>
          </div>
          <div class="step-card step-card-featured">
            <div class="step-badge-num">3</div>
            <span class="step-eyebrow">step 3 · 30 seconds 🤖</span>
            <h3 class="step-title">Your agent POSTs</h3>
            <p class="step-desc">One Bearer-authed POST and the submission lands in your inbox, tagged with the agent's label.</p>
            <a class="step-link" href="/docs">→ docs#fetch</a>
          </div>
        </div>
      </div>
    </section>

    <!-- ============ CORE FEATURES LIST ============ -->
    <section class="core-features-section">
      <div class="container">
        <div class="section-tag-row">
          <span class="red-eyebrow">/ WHY AGENTS PICK THIS</span>
          <h2 class="main-section-title">Core features, on every plan.</h2>
          <p class="main-section-sub">Designed for AI-agent traffic from the ground up.</p>
        </div>
        <div class="features-list-grid">
          <div class="feature-item-row">
            <div class="feat-title">Bearer tokens built for agents</div>
            <div class="feat-desc">Agent-typed tokens with a label, an audit log of every call, and a 🤖 stamp on the submissions they create. Scoped to your account. Revocable in one click.</div>
          </div>
          <div class="feature-item-row">
            <div class="feat-title">Spam pipeline that respects authorisation</div>
            <div class="feat-desc">Honeypot, rate limits, and CAPTCHA apply to anonymous browser traffic. Bearer-authed agent submissions skip them — the token is the trust boundary.</div>
          </div>
          <div class="feature-item-row">
            <div class="feat-title">Never lose a lead</div>
            <div class="feat-desc">Nothing is silently dropped. Every submission blocked by spam or validation is logged and reviewable — un-flag and re-send in one click.</div>
          </div>
          <div class="feature-item-row">
            <div class="feat-title">Self-describing API</div>
            <div class="feat-desc">Public <code>/api/openapi.json</code> — CORS-open, machine-readable. Agent frameworks consume it directly to discover endpoints and response shapes.</div>
          </div>
          <div class="feature-item-row">
            <div class="feat-title">Idempotency + signed webhooks</div>
            <div class="feat-desc">Idempotency keys on every submit. HMAC-signed webhooks with full delivery history, auto-retry with backoff, and one-click replay.</div>
          </div>
          <div class="feature-item-row">
            <div class="feat-title">Email + 1,000+ integrations</div>
            <div class="feat-desc">Slack, Discord, Telegram, Google Sheets, Mailchimp native — no Zapier subscription. Anything else via Zapier, Make, n8n, Activepieces.</div>
          </div>
        </div>
      </div>
    </section>

    <!-- ============ PRICING SECTION ============ -->
    <section class="pricing-section" id="pricing">
      <div class="container">
        <div class="section-tag-row">
          <span class="red-eyebrow">/ PRICING</span>
          <h2 class="main-section-title">Simple, transparent pricing.</h2>
          <p class="main-section-sub">Every tier includes the same core features — the monthly submission cap is the main difference. No overage charges.</p>
        </div>

        <div class="pricing-cards-grid">
          <!-- Free Plan -->
          <div class="pricing-plan-card">
            <div class="plan-head">
              <h3 class="plan-name">Free</h3>
              <p class="plan-desc">For testing & personal sites</p>
              <div class="plan-price">
                <span class="currency">$</span>
                <span class="amount">0</span>
                <span class="period">/ month</span>
              </div>
            </div>
            <ul class="plan-features">
              <li>✓ <strong>50 submissions</strong> / month</li>
              <li>✓ Unlimited forms</li>
              <li>✓ GEO-stamped leads</li>
              <li>✓ 1 agent token</li>
              <li>✓ Spam filtering</li>
            </ul>
            <a href={`${APP}/register`} class="btn btn-white-pill btn-full">Get started free</a>
          </div>

          <!-- Solo Plan -->
          <div class="pricing-plan-card featured-plan">
            <div class="popular-ribbon">Most Popular</div>
            <div class="plan-head">
              <h3 class="plan-name">Solo</h3>
              <p class="plan-desc">For indie hackers & builders</p>
              <div class="plan-price">
                <span class="currency">$</span>
                <span class="amount">9</span>
                <span class="period">/ month</span>
              </div>
            </div>
            <ul class="plan-features">
              <li>✓ <strong>500 submissions</strong> / month</li>
              <li>✓ Unlimited forms & webhooks</li>
              <li>✓ Full Agent Mail & Test Inboxes</li>
              <li>✓ 5 agent tokens + audit log</li>
              <li>✓ Slack & Discord integrations</li>
              <li>✓ AI Lead Enrichment built-in</li>
            </ul>
            <a href={`${APP}/register`} class="btn btn-dark-pill btn-full">Start 14-day trial</a>
          </div>

          <!-- Team Plan -->
          <div class="pricing-plan-card">
            <div class="plan-head">
              <h3 class="plan-name">Team</h3>
              <p class="plan-desc">For fast-growing agent apps</p>
              <div class="plan-price">
                <span class="currency">$</span>
                <span class="amount">29</span>
                <span class="period">/ month</span>
              </div>
            </div>
            <ul class="plan-features">
              <li>✓ <strong>5,000 submissions</strong> / month</li>
              <li>✓ Unlimited team members</li>
              <li>✓ Unlimited agent tokens</li>
              <li>✓ Priority support & custom domains</li>
              <li>✓ Dedicated IP for Agent Mail</li>
            </ul>
            <a href={`${APP}/register`} class="btn btn-white-pill btn-full">Get Team plan</a>
          </div>
        </div>
      </div>
    </section>

    <!-- ============ CTA BANNER ============ -->
    <section class="cta-bottom-section">
      <div class="container">
        <div class="cta-inner-box">
          <h2 class="cta-heading">Ready to give your AI agents a real backend?</h2>
          <p class="cta-sub">Get started in two minutes. No credit card required.</p>
          <div class="cta-buttons-wrap">
            <a href={`${APP}/register`} class="btn btn-dark-pill btn-hero">
              Start free <span class="arrow">→</span>
            </a>
            <a href="/docs" class="btn btn-white-pill btn-hero">
              Read the docs
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- ============ FOOTER ============ -->
  <footer class="site-footer">
    <div class="container footer-layout">
      <div class="footer-brand-col">
        <div class="logo-lockup">
          <div class="logo-box">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
              <path d="M4 7C7 5 11 5 14 7C17 9 20 8 20 8" stroke="white" stroke-width="2.5" stroke-linecap="round"/>
              <path d="M4 12C7 10 11 10 14 12C17 14 20 13 20 13" stroke="white" stroke-width="2.5" stroke-linecap="round"/>
              <path d="M4 17C7 15 11 15 14 17C17 19 20 18 20 18" stroke="white" stroke-width="2.5" stroke-linecap="round"/>
            </svg>
          </div>
          <span class="wordmark">ollastack</span>
        </div>
        <p class="footer-tagline">The complete developer surface for AI agents — forms, real mailboxes, and disposable test inboxes.</p>
        <p class="footer-copyright">© 2026 Ollastack Inc. All rights reserved.</p>
      </div>

      <div class="footer-links-col">
        <h5 class="footer-col-title">Product</h5>
        <ul class="footer-links">
          <li><a href="#product">Headless Forms</a></li>
          <li><a href="/email-api">Agent Mail</a></li>
          <li><a href="#live-demo">Test Inboxes</a></li>
          <li><a href="/pricing">Pricing</a></li>
        </ul>
      </div>

      <div class="footer-links-col">
        <h5 class="footer-col-title">Developers</h5>
        <ul class="footer-links">
          <li><a href="/docs">Documentation</a></li>
          <li><a href={`${APP}/api/openapi.json`}>OpenAPI Spec</a></li>
          <li><a href="#api-console">API Sandbox</a></li>
          <li><a href="/use-cases">Use Cases</a></li>
        </ul>
      </div>

      <div class="footer-links-col">
        <h5 class="footer-col-title">Company</h5>
        <ul class="footer-links">
          <li><a href="/blog">Blog</a></li>
          <li><a href="/privacy">Privacy Policy</a></li>
          <li><a href="/terms">Terms of Service</a></li>
          <li><a href="mailto:support@ollastack.com">Contact Support</a></li>
        </ul>
      </div>
    </div>
  </footer>

  <!-- CLIENT-SIDE SCRIPT -->
  <script is:inline>
    (function () {
      'use strict';

      // 1. Mobile Navigation Toggle
      const burger = document.getElementById('nav-burger');
      const mobileNav = document.getElementById('nav-mobile');
      if (burger && mobileNav) {
        burger.addEventListener('click', (e) => {
          e.stopPropagation();
          const isOpen = mobileNav.classList.toggle('open');
          burger.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
        });

        document.addEventListener('click', (e) => {
          if (mobileNav.classList.contains('open') && !mobileNav.contains(e.target) && e.target !== burger) {
            mobileNav.classList.remove('open');
            burger.setAttribute('aria-expanded', 'false');
          }
        });
      }

      // 2. Interactive Live Demo Form Submission
      const demoForm = document.getElementById('hero-interactive-form');
      const demoSubmitBtn = document.getElementById('demo-submit-button');
      const liveLeadId = document.getElementById('live-lead-id');
      const liveTimeVal = document.getElementById('live-time-value');
      const liveLocVal = document.getElementById('live-loc-value');

      function generateLeadId() {
        const chars = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ';
        let id = 'lead_01JX';
        for (let i = 0; i < 14; i++) {
          id += chars.charAt(Math.floor(Math.random() * chars.length));
        }
        return id;
      }

      function getFormattedCurrentTime() {
        const now = new Date();
        const options = {
          month: 'short',
          day: 'numeric',
          year: 'numeric',
          hour: 'numeric',
          minute: '2-digit',
          hour12: true,
          timeZoneName: 'short'
        };
        return now.toLocaleString('en-US', options);
      }

      if (demoForm) {
        demoForm.addEventListener('submit', (e) => {
          e.preventDefault();
          const origText = demoSubmitBtn.innerHTML;
          demoSubmitBtn.innerHTML = 'Submitting…';
          demoSubmitBtn.disabled = true;
          demoSubmitBtn.style.opacity = '0.7';

          setTimeout(() => {
            if (liveLeadId) liveLeadId.textContent = generateLeadId();
            if (liveTimeVal) liveTimeVal.textContent = getFormattedCurrentTime();

            demoSubmitBtn.innerHTML = '✓ Submitted!';
            demoSubmitBtn.style.background = '#059669';
            demoSubmitBtn.style.borderColor = '#059669';

            const outputCard = document.getElementById('demo-output-display');
            if (outputCard) {
              outputCard.style.transform = 'scale(1.02)';
              outputCard.style.boxShadow = '0 12px 32px rgba(5, 150, 105, 0.15)';
              setTimeout(() => {
                outputCard.style.transform = 'scale(1)';
                outputCard.style.boxShadow = '';
              }, 400);
            }

            setTimeout(() => {
              demoSubmitBtn.innerHTML = origText;
              demoSubmitBtn.style.background = '';
              demoSubmitBtn.style.borderColor = '';
              demoSubmitBtn.style.opacity = '1';
              demoSubmitBtn.disabled = false;
            }, 1800);
          }, 700);
        });
      }

      // Detect Live Geo Location for Demo
      fetch('https://ipapi.co/json/')
        .then(r => r.json())
        .then(data => {
          if (data && data.city && data.country_name) {
            const flag = data.country_code ? String.fromCodePoint(...[...data.country_code.toUpperCase()].map(c => 127397 + c.charCodeAt())) : '';
            if (liveLocVal) {
              liveLocVal.textContent = `${data.city}, ${data.country_name} ${flag}`;
            }
          }
        })
        .catch(() => {});

      // 3. API Sandbox Console Tabs
      const cTabBtns = document.querySelectorAll('.c-tab-btn');
      const cTabPanels = document.querySelectorAll('.c-tab-panel');
      cTabBtns.forEach(btn => {
        btn.addEventListener('click', () => {
          const tabKey = btn.dataset.tab;
          cTabBtns.forEach(b => {
            b.classList.toggle('active', b === btn);
            b.setAttribute('aria-selected', b === btn ? 'true' : 'false');
          });
          cTabPanels.forEach(panel => {
            panel.classList.toggle('active', panel.dataset.tab === tabKey);
          });
        });
      });

      // 4. API Sandbox Live Run Buttons
      const runFormsBtn = document.getElementById('run-forms-btn');
      const resFormsOut = document.getElementById('res-forms-output');
      if (runFormsBtn && resFormsOut) {
        runFormsBtn.addEventListener('click', () => {
          runFormsBtn.textContent = 'Running…';
          setTimeout(() => {
            resFormsOut.innerHTML = `<span class="c-ok">{
  "success": true,
  "id": "${generateLeadId()}",
  "country": "IN",
  "city": "Bengaluru",
  "timezone": "Asia/Kolkata",
  "geo_stamp": "verified",
  "created_at": "${new Date().toISOString()}"
}</span>`;
            runFormsBtn.textContent = '▶ Run it live';
          }, 500);
        });
      }

      const runTestBtn = document.getElementById('run-test-btn');
      const resTestOut = document.getElementById('res-test-output');
      if (runTestBtn && resTestOut) {
        runTestBtn.addEventListener('click', () => {
          runTestBtn.textContent = 'Waiting for email…';
          setTimeout(() => {
            resTestOut.innerHTML = `<span class="c-ok">{
  "status": "delivered",
  "inbox": "test_01HXY2@test.ollastack.com",
  "otp": "364921",
  "extracted_code": "364921",
  "assertion": "PASSED (took 142ms)"
}</span>`;
            runTestBtn.textContent = '▶ Run it live';
          }, 700);
        });
      }

      // 5. Code Integration Snippet Tabs
      const codeNavs = document.querySelectorAll('.code-tab-nav');
      const codeContents = document.querySelectorAll('.code-tab-content');
      codeNavs.forEach(nav => {
        nav.addEventListener('click', () => {
          const lang = nav.dataset.lang;
          codeNavs.forEach(n => n.classList.toggle('active', n === nav));
          codeContents.forEach(c => c.classList.toggle('active', c.dataset.lang === lang));
        });
      });

      // 6. Test Inbox Copy Action
      const copyInboxBtn = document.getElementById('btn-copy-test-inbox');
      if (copyInboxBtn) {
        copyInboxBtn.addEventListener('click', () => {
          navigator.clipboard.writeText('test_01HXY2@test.ollastack.com').then(() => {
            const orig = copyInboxBtn.textContent;
            copyInboxBtn.textContent = 'Copied!';
            setTimeout(() => { copyInboxBtn.textContent = orig; }, 1500);
          });
        });
      }
    })();
  </script>
</body>
</html>
